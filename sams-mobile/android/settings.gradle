pluginManagement {
    includeBuild("../node_modules/@react-native/gradle-plugin")

    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
    }
}

plugins {
    id("com.facebook.react") version("0.73.6") apply(false)
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "tempSAMS"

include(":app")

// Optional: Only include this if you're building native modules or using custom autolinks
includeBuild("../node_modules/@react-native/gradle-plugin")
